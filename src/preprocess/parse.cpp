#include <fstream>
#include <iostream>
#include <unordered_map>

#include "parse.h"

namespace MCA2 {
void Parser::parseConfigFile(std::string &configFilePath) {
    std::ifstream configFile(configFilePath);
}

void Parser::parseCalibXMLFile(std::string &calibXMLFilePath) {

}
} // namespace MCA2